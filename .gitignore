# build
dist/
.astro/

# dependencies
node_modules/

# Lighthouse reports - the raw results weigh several MB
docs/lighthouse/*.html
docs/lighthouse/*.json

# system
.DS_Store
Thumbs.db

# The Facebook refresh downloads into here and renames into src/assets/facebook/ only once
# every file has arrived. It never survives a finished run - see scripts/fetch-facebook.mjs.
.facebook-staging/

# local
.playwright-mcp/
.env
.env.production
*.log
